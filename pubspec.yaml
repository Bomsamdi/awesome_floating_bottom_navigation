name: awesome_floating_bottom_navigation
description: >-
  A floating bottom navigation bar with configurable item layout (expand or
  center), splash and scale animations, gradients, blur and custom borders.
version: 1.0.0
homepage: https://github.com/Bomsamdi/awesome_floating_bottom_navigation
repository: https://github.com/Bomsamdi/awesome_floating_bottom_navigation
issue_tracker: https://github.com/Bomsamdi/awesome_floating_bottom_navigation/issues

topics:
  - navigation
  - bottom-navigation
  - widget
  - animation
  - ui

environment:
  sdk: ^3.8.0
  flutter: '>=3.32.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
